# ==========================================
# GARMIN CONNECT CREDENTIALS
# ==========================================
# Used only during initial setup (npm run setup-garmin).
# After setup, authentication uses stored tokens.
GARMIN_EMAIL=your_garmin_email@example.com
GARMIN_PASSWORD=your_garmin_password

# ==========================================
# USER PROFILE
# ==========================================
USER_HEIGHT=183
USER_AGE=26
USER_GENDER=male
USER_IS_ATHLETE=true

# ==========================================
# SCALE CONFIGURATION
# ==========================================
# Your Renpho scale's Bluetooth MAC address.
# Run "npm run scan" to discover nearby BLE devices.
SCALE_MAC=FF:03:00:13:A1:04

# BLE Characteristic UUIDs (default works for most Renpho/generic scales)
CHAR_NOTIFY=0000fff1-0000-1000-8000-00805f9b34fb
CHAR_WRITE=0000fff2-0000-1000-8000-00805f9b34fb

# Hex bytes to unlock the scale data stream (comma-separated)
CMD_UNLOCK=0x13,0x09,0x00,0x01,0x01,0x02

# ==========================================
# TOKEN STORAGE
# ==========================================
# Directory to store Garmin auth tokens.
# Defaults to ~/.garmin_renpho_tokens if not set.
# TOKEN_DIR=~/.garmin_renpho_tokens
